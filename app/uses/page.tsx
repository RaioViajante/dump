import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Uses",
  description: "Hardware, software, and tools currently in rotation.",
};

export default function UsesPage() {
  return (
    <>
      <h1>Uses</h1>
      <p>
        A running list of the hardware, editor setup, and tools currently in
        rotation. To be filled in.
      </p>
    </>
  );
}
